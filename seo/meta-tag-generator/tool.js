document.addEventListener('DOMContentLoaded',()=>TBKTool.init(async H=>{
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); const title=H.v('title').trim(), d=H.v('description').trim(), canon=H.v('canonical').trim(), robots=H.v('robots'), img=H.v('image').trim(), social=H.checked('social'); if(!title||!d) return H.err('Add a title and description.'); let lines=[`<title>${esc(title)}</title>`,`<meta name="description" content="${esc(d)}">`,`<meta name="robots" content="${esc(robots)}">`,`<meta name="viewport" content="width=device-width, initial-scale=1">`]; if(canon) lines.push(`<link rel="canonical" href="${esc(canon)}">`); if(social){lines.push(`<meta property="og:title" content="${esc(title)}">`,`<meta property="og:description" content="${esc(d)}">`); if(canon) lines.push(`<meta property="og:url" content="${esc(canon)}">`); if(img) lines.push(`<meta property="og:image" content="${esc(img)}">`); lines.push(`<meta name="twitter:card" content="${img?'summary_large_image':'summary'}">`);} H.code(lines.join('\n')); H.note(`Title ${title.length} chars · Description ${d.length} chars`);
}));
