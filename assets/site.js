(()=>{const data=window.TBK_INDEX||[];const input=document.querySelector('[data-tool-search]'),box=document.querySelector('[data-search-results]');if(!input||!box)return;const render=()=>{const q=input.value.trim().toLowerCase();if(!q){box.classList.remove('show');box.innerHTML='';return;}const m=data.filter(x=>(x.name+' '+x.category+' '+x.desc).toLowerCase().includes(q)).slice(0,8);box.innerHTML=m.length?m.map(x=>`<a href="${x.url}"><span>${x.name}</span><small>${x.category}</small></a>`).join(''):'<a href="/tools/"><span>No exact match — browse all tools</span></a>';box.classList.add('show')};input.addEventListener('input',render);document.addEventListener('click',e=>{if(!box.contains(e.target)&&e.target!==input)box.classList.remove('show')})})();
